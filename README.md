# Streaming platform

In order to make the application work, it is required to have a directory at the path "/var/streaming_platform/videos_storage", that is made to store the videos.
The user has to have permissions to read from and write to said directory.
